---
alwaysApply: true
---

# a0-launcher Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-18

## Active Technologies
- Local filesystem (Electron `userData`) for caches and retention metadata; Docker for images/containers; optional secure blob via Electron `safeStorage` (001-docker-version-management)

- JavaScript (CommonJS), Node.js 20+, Electron 33.x (Electron Forge) (001-docker-version-management)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

JavaScript (CommonJS), Node.js 20+, Electron 33.x (Electron Forge): Follow standard conventions

## Recent Changes
- 001-docker-version-management: Added JavaScript (CommonJS), Node.js 20+, Electron 33.x (Electron Forge)

- 001-docker-version-management: Added JavaScript (CommonJS), Node.js 20+, Electron 33.x (Electron Forge)

<!-- MANUAL ADDITIONS START -->
## Manual Notes (Repo Reality)

- Repo structure is `app/`, `shell/`, `scripts/`, `specs/` (not `src/` and `tests/`).
- There is currently no standard `npm test` or `npm run lint` script. For fast checks, prefer `node --check` on individual files (for example: `node --check shell/main.js`).
<!-- MANUAL ADDITIONS END -->
